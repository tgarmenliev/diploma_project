{
    "name": "bultrain-backend",
    "version": "0.1.0",
    "scripts": {
      "postinstall": "cd backend && npm install && npm run build"
    },
    "engines": {
        "node": ">=20.10.0"
      }
  }